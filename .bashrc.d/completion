# Node Completion
if [ -d ~/.node-completion ]; then
  for file in ~/.node-completion/*
  do
    . $file
  done
fi

for file in ~/.node-completion/*
do
  . $file
done

if [ -f /etc/bash_completion ]; then
  . /etc/bash_completion
fi
